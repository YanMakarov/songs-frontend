# Base URL of the FastAPI backend
# Leave empty during `npm run dev` to route requests through the Vite proxy
# (see vite.config.js). Set this before `npm run build` or `npm run preview`.
VITE_API_BASE_URL=

# Default setlist slug for API requests
VITE_SETLIST_SLUG=setlist1
